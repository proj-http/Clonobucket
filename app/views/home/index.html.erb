<h1>Photo_Share&#8482;
<br>
  <small>
    &nbsp;&nbsp;Give us your <strike>data</strike> Pictures!
  </small>
</h1>
<p>

  <% @photos.each do |photo| %>
    <%= image_tag photo.image.url(:thumb) %>
    <%= photo.user.username %>

  <% end %>

</p>
